const { ARTIFACT_SETS } = require("../data/artifact-data.js");

// takes in the name of an artifact and returns the set that it belongs to
const getSetFromName = (name) => {
  return false;
}

module.exports = { getSetFromName };